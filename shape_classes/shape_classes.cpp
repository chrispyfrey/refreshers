class Circle {
    Circle(float r) {
        computeDiameter((double) r);
        computeCircumerence((double) r);
        computeArea((double) r);
    }

    private: 
        double radius = 0;
        double diameter = 0;
        double circumference = 0;
        double area = 0;

        double computeDiameter(double r) {
            
        };
        double computeCircumerence(double r) {
            
        };
        double computeArea(double r) {
        
        };

    public:
        double getRadius() {

        }
        double getDiameter() {

        }
        double getCircumerence() {

        }
        double getArea() {

        }
};